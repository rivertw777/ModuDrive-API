spring:
  application:
    name: member-service
  config:
    import:
      - "classpath:application-jpa.yml"
      - "classpath:application-observability.yml"
      - "classpath:application-sqs.yml"
      - "classpath:application-redis.yml"

modudrive:
  messaging:
    outbox:
      enabled: true
  member:
    email-verification-token-expiration: ${MEMBER_EMAIL_VERIFICATION_TOKEN_EXPIRATION:180000}

# Shared service-to-service secret, verified on this service's own /internal/** (see InternalTokenFilter).
internal:
  service:
    token: ${INTERNAL_SERVICE_TOKEN}
